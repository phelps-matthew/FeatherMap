from setuptools import setup

setup(
    name="feathermap",
    version="1.0",
    install_requires=[
        "torch",
        "torchvision",
        "packing",
        "numpy",
        "pandas",
        "matplotlib",
    ],
)
